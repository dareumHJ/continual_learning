# configs/experiment/dummy.yaml

method:
  name: dummy

modelpool:
  type: dummy
  models:
    - name: m1
  
taskpool:
  type: simple_classification
  dataset: mnist
  split: test
  batch_size: 64
  num_workers: 4